# accelerator-dse: Dataflow Strategy Comparison
Compare 3 dataflow strategies (WS/OS/RS) for energy, area, and performance on GEMM workloads. Includes roofline bottleneck analysis and sparse acceleration evaluation.
## Run
`cd mini-ai-accel-design && make && ./examples/example_ai_accel.exe`
